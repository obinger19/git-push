cd
sudo git clone Vizsga repo URL-je
sudo mc----> BEMÁSOLJUK A KÉRT FÁJLOKAT (tar fájlt) A LOKÁLIS REPOBA
ls -al
cd proba_umszki obingert19
sudo git add .
sudo git commit -m'elso'
sudo git config --global user.email "obinger.tamas96@gmail.com"
sudo git config --global user.name "Obinger Tamás"
sudo git commit -m'elso'
sudo git push
